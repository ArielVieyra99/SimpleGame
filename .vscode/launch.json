{
    "version":"0.2.0",
    "configurations": [

        {
            "name": "Run SFML",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/smallgame",
            "args": [],
            "cwd": "${fileDirname}",
            "MIMode": "lldb",
            "externalConsole": true,
            "preLaunchTask": "Build with SFML"
        }
    ]
}